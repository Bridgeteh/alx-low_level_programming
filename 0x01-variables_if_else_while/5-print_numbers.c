#include"stdio.h"
/**
* main - Prints numbers from 0 to 9.
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int b = 0;

	while (b < 1)
	{
        	printf("%d", b);
		b++;
	}
	putchar('\n');
	return (0);
}
